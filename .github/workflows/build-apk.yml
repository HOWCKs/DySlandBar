name: Build APK

# Builds on every push (any branch) so the working branch of a session always
# produces a downloadable APK. Manual runs are available via workflow_dispatch.
on:
  push:
  workflow_dispatch:

concurrency:
  group: build-apk-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build debug APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('**/*.gradle.kts', 'gradle/wrapper/gradle-wrapper.properties', 'gradle/libs.versions.toml') }}
          restore-keys: |
            gradle-${{ runner.os }}-

      - name: Build debug APK
        run: |
          rc=0
          ./gradlew :app:assembleDebug --no-daemon --stacktrace --console=plain > /tmp/gradle.log 2>&1 || rc=$?
          if [ $rc -ne 0 ]; then
            echo "::error::Gradle build failed (exit $rc) — last 45 lines of the log:"
            tail -n 45 /tmp/gradle.log | while IFS= read -r line; do
              echo "::error file=gradle.log::${line}"
            done
            exit 1
          fi
          tail -n 15 /tmp/gradle.log

      - name: Prepare artifact
        run: |
          SHA=$(git rev-parse --short HEAD)
          DATE=$(date -u +%Y%m%d)
          VERSION=$(grep -oP 'versionName = "\K[^"]+' app/build.gradle.kts)
          mkdir -p dist
          cp app/build/outputs/apk/debug/app-debug.apk "dist/DynamicMaterialIsland-v${VERSION}-${DATE}-${SHA}.apk"
          ls -lh dist

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: DynamicMaterialIsland-apk
          path: dist/*.apk
          retention-days: 30
          if-no-files-found: error
